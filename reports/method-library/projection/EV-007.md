# EV-007 Projection, Trace, and Read-Only Report

Date: 2026-05-27
Phase coverage: PH-06 query, trace, and rebuild flows

## Scope

- Validate query paths stay read-only.
- Validate projection rebuild and trace retrieval behavior.
- Validate projection staleness is observable instead of being hidden by truth writes.

## Covered Cases

| TC ID | Coverage |
|---|---|
| TC-QRY-001 | `GetMethodContent` is read-only. |
| TC-QRY-002 | `ListMethodContents` pagination and consistency markers are stable. |
| TC-QRY-007 | `GetDefinitionTrace` returns version, lifecycle, audit, outbox, and supersede-chain data. |
| TC-JOB-004 | Rebuild reconstructs projections without rewriting truth. |
| AC-BND-005 | Projection rebuilds and trace reads do not mutate definition truth. |

## Execution

```bash
cargo test -p method_library_application query_services::tests
cargo test -p method_library_application operations_services::tests::rebuild_reconstructs_summary_and_trace_projections
```

## Evidence Notes

- Trace responses are backed by read projections and write-model history, not by repair-on-read behavior.
- Rebuild dry-run coverage is recorded again in EV-009.
