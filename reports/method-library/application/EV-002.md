# EV-002 Command Service Test Report

Date: 2026-05-27
Phase coverage: PH-04 draft and review entry flows

## Scope

- Validate `CreateMethodContentDraft`, `UpdateMethodContentDraft`, and `SubmitMethodContentForReview`.
- Validate idempotent command replay for draft and review operations.
- Validate HTTP entry wiring for the same command family.

## Covered Cases

| TC ID | Coverage |
|---|---|
| TC-CMD-001 | Draft creation succeeds with lifecycle, audit, and idempotency records. |
| TC-CMD-002 | Draft update replaces payload and references atomically. |
| TC-CMD-003 | Published content cannot be updated through the draft path. |

## Execution

```bash
cargo test -p method_library_application services::tests
cargo test -p method_library_api routes::tests
```

## Evidence Notes

- Application-level coverage verifies orchestration and side effects.
- API-level coverage verifies request extraction, validation, and response mapping for the same command slice.
