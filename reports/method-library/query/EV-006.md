# EV-006 Snapshot Export and ResolveViewProfile Report

Date: 2026-05-27
Phase coverage: PH-06 read-only snapshot and view-profile flows

## Scope

- Validate immutable snapshot export.
- Validate unique `ResolveViewProfile` resolution and ambiguous-match failure behavior.

## Covered Cases

| TC ID | Coverage |
|---|---|
| TC-QRY-003 | Snapshot export returns immutable content and references. |
| TC-QRY-004 | Snapshot fingerprint mismatches fail instead of being silently ignored. |
| TC-QRY-005 | Unique published view-profile matches resolve successfully. |
| TC-QRY-006 | Ambiguous view-profile matches fail with a stable error. |
| AC-P0-006 | Snapshot export remains consumable for downstream use. |
| AC-P0-007 | `ResolveViewProfile` stays server-authoritative. |

## Execution

```bash
cargo test -p method_library_application query_services::tests::exports_definition_snapshots_from_object_storage
cargo test -p method_library_application query_services::tests::rejects_snapshot_exports_with_fingerprint_mismatch
cargo test -p method_library_application query_services::tests::resolves_unique_view_profiles_from_published_content
cargo test -p method_library_application query_services::tests::rejects_ambiguous_view_profile_matches
```

## Evidence Notes

- Snapshot export is verified with fingerprint validation enabled.
- View-profile resolution remains read-only and does not persist local authority rules.
