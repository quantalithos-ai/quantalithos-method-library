# EV-004 Fingerprint, Version, and Snapshot Report

Date: 2026-05-27
Phase coverage: PH-05 publish and supersede flows

## Scope

- Validate that publish and supersede produce immutable version, fingerprint, and snapshot records.
- Validate fingerprint drift detection and snapshot fingerprint mismatch reporting.

## Covered Cases

| TC ID | Coverage |
|---|---|
| TC-DOM-005 | Canonical fingerprint output is stable. |
| TC-DOM-006 | Semantic payload changes produce fingerprint changes. |
| TC-CMD-004 | Publish creates version, fingerprint, and snapshot artifacts. |
| TC-CMD-009 | Supersede creates a replacement version and snapshot while preserving lineage. |
| TC-JOB-003 | Fingerprint recalculation reports mismatches without mutating truth. |
| TC-QRY-004 | Snapshot export detects fingerprint mismatches and fails safely. |

## Execution

```bash
cargo test -p method_library_application services::tests::publishes_draft_and_reuses_idempotent_result
cargo test -p method_library_application services::tests::supersedes_old_content_and_publishes_replacement
cargo test -p method_library_application operations_services::tests::recalculate_reports_mismatches_without_mutating_published_truth
cargo test -p method_library_application query_services::tests::rejects_snapshot_exports_with_fingerprint_mismatch
```

## Evidence Notes

- Fingerprint generation is driven by the command service and the deterministic fingerprint policy.
- Snapshot integrity is enforced again on query export, not just at publish time.
