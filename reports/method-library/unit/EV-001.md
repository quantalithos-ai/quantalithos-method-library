# EV-001 Domain and Policy Unit Test Report

Date: 2026-05-27
Phase coverage: PH-02 baseline, reused by PH-08 acceptance packaging

## Scope

- Validate payload-kind matching across the 7 P0 `MethodContent` variants.
- Validate lifecycle baseline rules and domain-side boundary guards.
- Validate fingerprint canonicalization and semantic drift behavior at the unit level.

## Covered Cases

| TC ID | Coverage |
|---|---|
| TC-DOM-001 | Valid payloads match their declared content kind. |
| TC-DOM-002 | Payload-kind mismatches return `PAYLOAD_KIND_MISMATCH`. |
| TC-DOM-003 | Definition references enforce published-only targets. |
| TC-DOM-004 | Forbidden Use truth fields trigger `BOUNDARY_VIOLATION`. |
| TC-DOM-005 | Canonical fingerprint generation is stable for equivalent payloads. |
| TC-DOM-006 | Semantic payload changes change the fingerprint. |
| TC-STATE-001 | Draft and review lifecycle baseline remains valid in the domain model. |

## Execution

```bash
cargo test -p method_library_domain -p method_library_contracts
```

## Evidence Notes

- This report covers the pure domain and contract layer only; no adapter, transaction, or HTTP behavior is included.
- Downstream boundary validation is reinforced later by EV-003 and EV-010.
